<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


    <style name="AppTheme.Text" parent="AppTheme">
        <item name="android:textSize">@dimen/textRegular</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="AppTheme.Text.Header">
        <item name="android:textSize">@dimen/textHeader</item>
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.Text.SubHeader">
        <item name="android:textSize">@dimen/textSubHeader</item>
        <item name="android:textColor">@color/textSecondary</item>
        <item name="android:fontFamily">sans-serif-thin</item>
    </style>

    <style name="AppTheme.Text.Error">
        <item name="android:textSize">@dimen/errorHeader</item>
        <item name="android:textColor">@color/textPrimaryInverse</item>
        <item name="android:fontFamily">sans-serif-thin</item>
    </style>
    <style name="AppTheme.Button">
        <item name="android:textAllCaps">true</item>
        <item name="android:background">@color/buttonBg</item>
        <item name="android:textColor">@color/textPrimaryInverse</item>
    </style>
    <style name="AppTheme.Loader">
        <item name="android:layout_width">@dimen/loader</item>
        <item name="android:layout_height">@dimen/loader</item>
    </style>
</resources>
